# Template for release-mode E2E testing of the eks-oidc template.
# Placeholders are replaced at build time by the Dockerfile:
#   {{PKG_VERSION}}     — version to pin from EXTRA_INDEX_URL
#   {{EXTRA_INDEX_URL}} — index for the package under test (default: Test PyPI)
[project]
name = "e2e-eks-oidc-release"
version = "0.0.0"
requires-python = ">=3.12"
dependencies = [
    "jupyter-deploy[aws,k8s]",
    "pytest-jupyter-deploy[ui]",
    "pytest-cov",
    "jupyter-deploy-tf-aws-eks-oidc=={{PKG_VERSION}}",
]

[[tool.uv.index]]
name = "extra"
url = "{{EXTRA_INDEX_URL}}"
explicit = true

[tool.uv.sources]
jupyter-deploy-tf-aws-eks-oidc = { index = "extra" }
pytest-jupyter-deploy = { path = "libs/pytest-jupyter-deploy", editable = true }
