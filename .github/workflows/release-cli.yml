name: Release CLI (jupyter-deploy)

run-name: Release jupyter-deploy

on:
  workflow_dispatch:

permissions:
  id-token: write
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.get-version.outputs.version }}
      pkg-name: ${{ steps.get-version.outputs.pkg-name }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Install UV and Python
        uses: ./.github/actions/install-uv
        with:
          python-version: '3.13'
          working-directory: '.'

      - name: Build package
        run: |
          cd libs/jupyter-deploy
          uv build

      - name: Get package version
        id: get-version
        run: |
          cd libs/jupyter-deploy
          echo version=$(uv version --short) >> $GITHUB_OUTPUT
          echo pkg-name="$(uv version | cut -d ' ' -f 1)" >> $GITHUB_OUTPUT

      - name: Upload build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: dist
          path: dist/
          retention-days: 7

  lint:
    name: Run linters
    needs: [build]
    uses: ./.github/workflows/lint.yml
    with:
      working-directory: libs/jupyter-deploy

  test:
    name: Run tests
    needs: [lint]
    uses: ./.github/workflows/test.yml
    with:
      working-directory: libs/jupyter-deploy

  prepublish-smoke:
    name: "Pre-publish smoke (${{ matrix.variant }})"
    needs: [test, build]
    runs-on: ubuntu-latest
    timeout-minutes: 20
    strategy:
      fail-fast: false
      matrix:
        variant: [bare, aws, aws-k8s]
    env:
      VERSION: ${{ needs.build.outputs.version }}
    steps:
      - uses: actions/checkout@v4

      - name: Install UV and Python
        uses: ./.github/actions/install-uv
        with:
          python-version: '3.13'

      - name: Install just
        uses: ./.github/actions/install-just

      - name: Download build artifacts
        uses: actions/download-artifact@v4
        with:
          name: dist
          path: dist

      # Build the release image from the locally-built wheel (flat file index),
      # exactly as users install it (INSTALL_MODE=pypi) but without any publish.
      # Catches packaging-metadata, missing-file, and test-selector regressions
      # before a version is burned on Test PyPI.
      - name: Build CLI E2E image from local wheel
        run: |
          BUILD_ARGS="--build-arg INSTALL_MODE=pypi"
          BUILD_ARGS="$BUILD_ARGS --build-arg INSTALL_VARIANT=${{ matrix.variant }}"
          BUILD_ARGS="$BUILD_ARGS --build-arg PKG_VERSION=${{ env.VERSION }}"
          BUILD_ARGS="$BUILD_ARGS --build-arg EXTRA_INDEX_URL=file:///workspace/dist"
          BUILD_ARGS="$BUILD_ARGS --build-arg INDEX_FORMAT=flat"
          just ci-e2e-cli-build "" "$BUILD_ARGS"

      - name: Run smoke tests
        run: just test-smoke-cli ${{ matrix.variant }} jupyter-deploy-e2e-cli:latest

  test-pypi-publish:
    name: Publish to Test PyPI
    needs: [test, build, prepublish-smoke]
    runs-on: ubuntu-latest
    permissions:
      id-token: write
    environment:
      name: test-release
    steps:
      - name: Download build artifacts
        uses: actions/download-artifact@v4
        with:
          name: dist
          path: dist

      - name: List build artifacts
        run: ls -la dist/

      - name: Setup UV
        uses: astral-sh/setup-uv@v3

      - name: Publish to Test PyPI
        run: |
          uv publish \
            --publish-url https://test.pypi.org/legacy/ \
            --check-url https://test.pypi.org/simple \
            --trusted-publishing always

  test-pypi-install:
    name: Check installation of the Test PyPI build
    needs: [test-pypi-publish, build]
    runs-on: ubuntu-latest
    env:
      PKG_NAME: ${{ needs.build.outputs.pkg-name }}
      VERSION: ${{ needs.build.outputs.version }}
    steps:
      - name: Setup UV
        uses: astral-sh/setup-uv@v3

      - name: Wait for Test PyPI availability
        run: |
          url="https://test.pypi.org/pypi/${PKG_NAME}/${VERSION}/json"
          echo "Polling ${url}"
          for i in $(seq 1 20); do
            if curl -fsS -o /dev/null "${url}"; then
              echo "${PKG_NAME}==${VERSION} is available on Test PyPI"
              exit 0
            fi
            echo "Attempt ${i}/20: not yet available, retrying in 15s..."
            sleep 15
          done
          echo "::error::${PKG_NAME}==${VERSION} did not appear on Test PyPI within 5 minutes"
          exit 1

      - name: Test installation from Test PyPI
        run: |
          uv venv
          uv pip install \
            --extra-index-url https://test.pypi.org/simple/ \
            --index-strategy unsafe-best-match \
            ${{ env.PKG_NAME }}==${{ env.VERSION }}

  e2e-cli-release:
    name: E2E release gate (CLI)
    needs: [test-pypi-install, build]
    uses: ./.github/workflows/e2e-cli.yml
    secrets: inherit
    with:
      pkg-version: ${{ needs.build.outputs.version }}

  prod-pypi-publish:
    name: Publish to PyPI
    needs: [e2e-cli-release, build]
    runs-on: ubuntu-latest
    permissions:
      id-token: write
    environment:
      name: release
    steps:
      - name: Download build artifacts
        uses: actions/download-artifact@v4
        with:
          name: dist
          path: dist

      - name: List build artifacts
        run: ls -la dist/

      - name: Setup UV
        uses: astral-sh/setup-uv@v3

      - name: Publish to PyPI
        run: |
          uv publish \
            --trusted-publishing always

  mark-release:
    name: Create GitHub Release
    needs: [prod-pypi-publish, build]
    if: always() && needs.prod-pypi-publish.result == 'success'
    runs-on: ubuntu-latest
    permissions:
      contents: write
    env:
      PKG_NAME: ${{ needs.build.outputs.pkg-name }}
      VERSION: ${{ needs.build.outputs.version }}
    steps:
      - uses: actions/checkout@v4

      - name: Install UV and Python
        uses: ./.github/actions/install-uv
        with:
          python-version: '3.13'
          working-directory: '.'

      - name: Download build artifacts
        uses: actions/download-artifact@v4
        with:
          name: dist
          path: dist/

      - name: Create Release
        uses: ncipollo/release-action@v1
        with:
          artifacts: "dist/*"
          token: ${{ secrets.GITHUB_TOKEN }}
          draft: false
          generateReleaseNotes: true
          tag: ${{ env.PKG_NAME }}==${{ env.VERSION }}
          commit: ${{ github.sha }}
