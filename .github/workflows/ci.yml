name: ci

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest
    outputs:
      affected_dirs: ${{ steps.get-dirs.outputs.affected_dirs }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Install UV and Python
        uses: ./.github/actions/install-uv
        with:
          python-version: '3.13'
          working-directory: '.'

      - name: Get affected directories
        id: get-dirs
        run: |
          chmod +x .github/utils/get_modified_dirs.py

          if [[ "${{ github.event_name }}" == "pull_request" ]]; then
            BASE_REF="HEAD^"
          else
            BASE_REF=""
          fi

          AFFECTED_DIRS=$(uv run .github/utils/get_modified_dirs.py "$BASE_REF")
          echo "affected_dirs=$AFFECTED_DIRS" >> $GITHUB_OUTPUT
          echo "Affected directories: $AFFECTED_DIRS"

  lint:
    name: Run linters
    needs: build
    strategy:
      matrix:
        working-directory: ${{ fromJson(needs.build.outputs.affected_dirs) }}
    uses: ./.github/workflows/lint.yml
    with:
      working-directory: ${{ matrix.working-directory }}

  test:
    name: Run tests
    needs: build
    strategy:
      matrix:
        working-directory: ${{ fromJson(needs.build.outputs.affected_dirs) }}
    uses: ./.github/workflows/test.yml
    with:
      working-directory: ${{ matrix.working-directory }}

  # The client-proxy functional suite is excluded from the per-dir `test` run (root
  # `norecursedirs`), so it runs here on every PR as its own job.
  functional-test:
    name: Run functional tests
    uses: ./.github/workflows/functional-test.yml

  docs-sync:
    name: Verify CLI reference docs are in sync
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install UV and Python
        uses: ./.github/actions/install-uv
        with:
          python-version: '3.13'
          working-directory: '.'

      - name: Install dependencies
        run: |
          rm -f uv.lock
          uv sync --all-extras

      - name: Verify generated CLI reference docs
        run: |
          uv run python scripts/verify_docs.py
