# General

Deploy interactive applications to the cloud.

**Usage**:

```console
$ jd [OPTIONS] COMMAND [ARGS]...
```

**Options**:

* `-V, --version`: Show the version.
* `--install-completion`: Install completion for the current shell.
* `--show-completion`: Show completion for the current shell, to copy it or customize the installation.
* `--help`: Show this message and exit.

**Commands**:

* `init`: Initialize a project from a template in...
* `config`: Verify the system configuration, prompt...
* `up`: Apply the project configuration to the...
* `down`: Destroy the cloud resources defined in the...
* `open`: Open the app in your web browser.
* `show`: Display information about the project.
* `health`: Check the health of the full deployment...
* `users`: Control access to your app at user level.
* `teams`: Control access to your app at team level.
* `organization`: Control access to your app at the...
* `server`: Interact with the server(s) running your...
* `component`: Interact with the platform components...
* `host`: Interact with the host machine(s) running...
* `cluster`: Interact with the cluster managing the...
* `history`: View and manage logs emitted by the...
* `projects`: Manage projects saved in remote stores.
