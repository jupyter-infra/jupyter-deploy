# JD internal state and outputs
.jd-history/
.jd-proxy/
jdout-*
jdinputs.*

# Terraform state files
.terraform/
*.tfstate
*.tfstate.*
.terraform.lock.hcl

# Python virtual environment — reproducible locally, too large to store remotely
.venv/
