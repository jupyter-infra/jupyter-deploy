# E2E Test Configuration
# Copy this file to .env (repository root) and fill in your values
# The docker-compose will automatically load .env from the repository root

# ==============================================================================
# TEST CONTAINER BUILD CONFIGURATION
# ==============================================================================

# Leave blank the host settings, the justfile methods will populate them
HOST_UID=
HOST_GID=


# ==============================================================================
# JUPYTER DEPLOYMENT CONFIGURATION
# ==============================================================================

# Domain configuration
JD_E2E_VAR_DOMAIN=example.com
JD_E2E_VAR_SUBDOMAIN=test-e2e
JD_E2E_VAR_EMAIL=test@example.com

# OAuth configuration
JD_E2E_VAR_OAUTH_APP_CLIENT_ID=00000aaaaa11111bbbbb
JD_E2E_VAR_OAUTH_APP_CLIENT_SECRET=00000aaaaa11111bbbbb22222ccccc

# Access control (at least one must be set)
# Array variables use YAML list syntax:
#   - Empty list: []
#   - Single item: ["username1"]
#   - Multiple items: ["username1", "username2"]
JD_E2E_VAR_OAUTH_ALLOWED_USERNAMES=["username1", "username2"]
# Required when JD_E2E_VAR_OAUTH_ALLOWED_TEAMS is not []
JD_E2E_VAR_OAUTH_ALLOWED_ORG=your-org-name
# Array variables use YAML list syntax:
#   - Empty list: []
#   - Single item: ["team1"]
#   - Multiple items: ["team1", "team2"]
JD_E2E_VAR_OAUTH_ALLOWED_TEAMS=[]

# EKS admin roles (used by eks-oidc template)
# IAM role names granted EKS cluster admin access.
# Add a DeployRole to work around permission boundaries locally.
JD_E2E_VAR_ADMIN_ROLE_NAMES=["Admin"]

# ==============================================================================
# TEST CONFIGURATION
# ==============================================================================

# --- USERS TESTS ---
# GitHub username the current webbrowser is logged as.
JD_E2E_USER=

# GitHub username that the current browser is not logged as
# but that you trust to grant access to your app.
# The e2e tests will assert that your user may not access the app
# when only the safe user is allowlisted.
JD_E2E_SAFE_USER=
# ---

# --- ORG AND TEAM TESTS ---
# Name of a GitHub organization the user is a member of.
JD_E2E_ORG=

# Name of an organization the logged user is not a member of,
# but that you trust to grant access to your app.
# The e2e tests will assert that your user may no longer access the app
# based on their org membership.
JD_E2E_SAFE_ORG=

# Name of a GitHub team within the organization that the logged user is a member of (OAuth app access).
JD_E2E_TEAM=

# Name of a GitHub team with workspace RBAC permissions (github-rbac chart RoleBinding).
# Used for impersonation in EKS template tests.
JD_E2E_RBAC_TEAM=

# Name of a GitHub team within the organization that the logged user is not a member of,
# but that you trust to grant access to your app.
# The e2e tests will assert that your user may no longer access the app
# based on their team's membership.
JD_E2E_SAFE_TEAM=
# ---

# --- CONFIG CHANGE TESTS ---
# Larger instance type for testing instance upgrades (e.g., t3.xlarge).
JD_E2E_LARGER_INSTANCE=

# Larger log retention days for testing config changes (e.g., 365).
JD_E2E_LARGER_LOG_RETENTION_DAYS=

# CPU instance type for testing instance swaps (e.g., t3.medium).
JD_E2E_CPU_INSTANCE=

# GPU instance type for testing GPU deployment (e.g., g6.xlarge).
JD_E2E_GPU_INSTANCE=
# ---
